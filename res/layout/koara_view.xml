<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <com.hayaoki.koara.KoaraView
    android:id="@+id/KoaraView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@+id/BottomButtonLayout"
  />

  <RelativeLayout
    android:id="@+id/BottomButtonLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true">

    <Button
      android:id="@+id/ItemButton"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_toLeftOf="@+id/CenterSpacer"
      android:text="アイテム"
    />

    <View
      android:id="@+id/CenterSpacer"
      android:layout_width="1px"
      android:layout_height="1px"
      android:layout_centerHorizontal="true"
    />

    <Button
      android:id="@+id/BgButton"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_toRightOf="@+id/CenterSpacer"
      android:text="背景画像切り替え"
    />
  </RelativeLayout>

  <ImageView
    android:id="@+id/DemoImage"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@+id/BottomButtonLayout"
  />
</RelativeLayout>